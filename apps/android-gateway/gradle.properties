# AndroidX is required: every dependency this module declares is AndroidX, and
# without the flag the build fails with "Configuration contains AndroidX
# dependencies, but the android.useAndroidX property is not enabled".
android.useAndroidX=true
android.nonTransitiveRClass=true

org.gradle.jvmargs=-Xmx2048m
