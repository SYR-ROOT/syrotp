{
  "name": "@syrotp/cli",
  "version": "0.2.0-pre.1",
  "description": "Command-line interface for SYROTP — operators run doctor / bootstrap / receiver / smoke / loadtest. PR 4 will rewrite the README quickstart around these.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "syrotp": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test --import tsx src/argv.test.ts src/config.test.ts src/exit.test.ts test/dispatcher.test.ts test/bootstrap.test.ts test/receiver.test.ts test/smoke.test.ts test/loadtest.test.ts"
  },
  "engines": {
    "node": ">=20.10.0"
  },
  "dependencies": {
    "@syrotp/server": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.1",
    "@types/node": "^22.7.5"
  }
}
