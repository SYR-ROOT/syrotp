name: syrotp_flutter_example
description: Minimal Flutter app demonstrating SyrotpVerificationWidget.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  syrotp_flutter:
    path: ../

dev_dependencies:
  flutter_lints: ^4.0.0
